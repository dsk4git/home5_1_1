// Пример 1. Подсчёт среднего значения
let algebra = 3;
let geography = 5;
let physics = 3;
let TotalSubjects = 3;
let AverageMark = (algebra + geography + physics) / TotalSubjects;
console.log("Среднее значение : " + AverageMark.toFixed(2));

// Пример 2. Вывод имени по шаблону
let myname = "Дмитрий";
let message = "Привет, мир! Меня зовут " + myname;
console.log(`${message}`);

// Пример 3. Вывод значения выражения
let x = 2;
let y = 22;
let z = 0;
let result;
result = x * y + 5 * z + x - 1;
console.log("Значение выражения: " + result);